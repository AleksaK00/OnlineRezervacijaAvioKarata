<div class="row mt-5">

    <nav class="navbar navbar-expand-lg bg-primary">
        <div class="container-fluid">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0 lead">
                    <li class="nav-item">
                        <a class="nav-link @((Context.Request.Path == ("/Admin/PregledKorisnika") || Context.Request.Path == ("/Admin/PretraziKorisnike")) ? "rounded-3 active" : "")"
                           asp-controller="Admin" asp-action="PregledKorisnika">Korisnici</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link @((Context.Request.Path == ("/Admin/PregledRezervacija")  || Context.Request.Path == ("/Admin/PretraziRezervacije")) ? "rounded-3 active" : "")"
                           asp-controller="Admin" asp-action="PregledRezervacija">Rezervacije</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link @((Context.Request.Path == ("/Admin/UpravljanjePromocijama")) ? "rounded-3 active" : "")"
                           asp-controller="Account" asp-action="UpravljanjePromocijama">Promocije</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link @((Context.Request.Path == ("/Admin/NoviKorisnik")) ? "rounded-3 active" : "")"
                           asp-controller="Account" asp-action="NoviKorisnik">Novi korisnik</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

</div>